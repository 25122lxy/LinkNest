# LinkNest （链接库）- 技术架构文档

## 1. 项目概览

### 1.1 项目信息
- **项目名称**: LinkNest
- **版本**: 1.1.0
- **描述**: 极简个人书签管理工具
- **语言**: 中文

### 1.2 项目目标
- 快速搜索书签
- 按标签筛选书签
- 一键跳转到目标链接

### 1.3 技术栈
| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 | ^3.4.0 |
| 构建 | Vite | ^5.0.0 |
| Vue 插件 | @vitejs/plugin-vue | ^5.0.0 |

### 1.4 项目约束
- 无后台系统
- 无数据库
- 无用户登录
- 纯静态 JSON 驱动

---

## 2. 技术选型

### 2.1 前端框架
- Vue 3 (Options API)
- Vite (构建工具)
- 原生 CSS（无 CSS 框架）

### 2.2 部署平台
- 优先: Cloudflare Pages
- 备选: Vercel

### 2.3 数据存储
- 单一 JSON 文件: `public/data.json`

---

## 3. 功能模块设计

### 3.1 书签展示 (BookmarkList.vue)
- 列表展示书签卡片
- 自动获取网站 favicon
- 悬停动画效果
- 空状态提示
- 复制链接按钮（悬停显示，点击复制 + Toast 提示）
- 移动端按钮默认显示

### 3.2 搜索功能 (SearchBar.vue)
- 关键词搜索
- 实时过滤（非防抖）
- Esc 清除搜索
- 按 `/` 聚焦搜索框

### 3.3 标签筛选 (HomeView.vue)
- 多标签组合筛选
- 选中状态高亮
- 清除筛选按钮
- 默认显示7个标签，超过显示"+N 更多"
- 展开/收起功能

### 3.4 数据处理 (useBookmarks.js)
- 数据加载（fetch）
- 搜索过滤
- 标签提取
- 组合筛选逻辑

---

## 4. 目录结构

```
.
├── public/
│   └── data.json          # 书签数据文件
├── src/
│   ├── components/
│   │   ├── BookmarkList.vue  # 书签列表组件
│   │   └── SearchBar.vue     # 搜索栏组件
│   ├── composables/
│   │   └── useBookmarks.js   # 书签数据处理逻辑
│   ├── views/
│   │   └── HomeView.vue      # 主页视图
│   ├── App.vue               # 主应用组件
│   └── main.js               # 应用入口
├── index.html                # HTML 入口
├── vite.config.js           # Vite 配置
└── package.json             # 项目配置
```

---

## 5. 组件说明

### 5.1 App.vue
- 全局样式（CSS 变量、滚动条）
- 挂载 HomeView

### 5.2 HomeView.vue
- 页面布局
- 状态管理
- 标签筛选逻辑

### 5.3 SearchBar.vue
- 搜索输入框
- v-model 双向绑定
- 键盘事件处理

### 5.4 BookmarkList.vue
- 书签卡片渲染
- favicon 获取逻辑
- URL 格式化显示
- 复制链接功能（clipboard API + Toast 提示）
- 响应式布局适配

### 5.5 useBookmarks.js (Composables)
- `bookmarks`: 原始数据
- `searchQuery`: 搜索词
- `selectedTags`: 选中标签
- `loading`: 加载状态
- `error`: 错误信息
- `allTags`: 所有标签列表
- `filteredBookmarks`: 过滤后数据

---

## 6. 数据流

```
public/data.json
       ↓ fetch()
useBookmarks.js
       ↓ computed
 HomeView.vue
       ↓ props
 BookmarkList.vue
       ↓ render
  书签卡片列表
```

---

## 7. 构建与部署

### 7.1 开发
```bash
npm install
npm run dev
```

### 7.2 构建
```bash
npm run build
```

### 7.3 预览
```bash
npm run preview
```

### 7.4 部署流程
1. 修改 `public/data.json`
2. 执行 `npm run build`
3. 将 `dist/` 目录部署到 Cloudflare Pages 或 Vercel

---

## 8. 后续扩展 (暂不实现)

- 接入在线数据库（如 Supabase）
- 数据分类功能
- 收藏/置顶功能
- 多设备同步